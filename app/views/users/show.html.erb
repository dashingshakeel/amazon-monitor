<% provide(:title, @user.name) %>
<div class="pad"><div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
      <div class="gravatar">
       <%= gravatar_for @user%></div><div class="name">
        <%= @user.name %></div>
      </h1>
    </section>
  </aside>
  <div class="computer"><a href="/groupitems"><%= image_tag("computer.png",:alt => "feed", class: "computer") %><p>
    <% if current_user %>
      <%=current_user.group.groupitems.count %>
    <% end %>
    
  </p> </a></div>
</div>
      <%= render 'shared/item_form' %>
      <div class="items"> <%= render 'shared/items'%></div>


</div>